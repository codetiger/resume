# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

An interactive résumé in two parts:

1. **Game (primary)** — a Three.js + TypeScript puzzle where a cube rolls across a tile grid.
   Entry point is the root `index.html`, loading `src/main.ts` via Vite.
2. **Static fallback** — a JSON-driven static HTML résumé generated by a Python/Jinja2 build,
   served at `public/index.html` (GitHub Pages) for no-JS/no-WebGL visitors.

The two stacks are independent: the game does not consume `resume.json`, and the Python builder
does not touch `src/`.

## Commands

Game:
- **Dev server:** `npm run dev` (Vite, http://localhost:5173)
- **Build:** `npm run build` (`tsc --noEmit` then `vite build` → `dist/`)

Static fallback:
- **Render résumé:** `python3 build.py` — renders `resume.json` + `template.html` → `public/index.html`
- **Install Python deps:** `pip3 install -r requirements.txt`

## Architecture

### Game (`src/`)
- `core.ts` — shared primitives used across modules: the `Direction` union, `DIRECTION_DELTA`
  grid steps, the deterministic `noise()` hash, and the `PALETTE` colour map. Add shared
  constants/colours here rather than re-declaring them per module.
- `engine/scene.ts` — Three.js scene, camera, lighting, parallax.
- `engine/models.ts` — loads + normalises the player/platform OBJ+MTL meshes (`loadNormalizedModel`).
- `game/grid.ts` — `buildLevel()` constructs the board, tile state, chain reactions (blasts/lines),
  and win/lose rules. `parseLayout()` turns a compact char grid into the level; `DEMO_LAYOUT` is the demo.
- `game/player.ts` — rolling/falling/teleporting cube physics.
- `game/tile.ts` — per-kind tile materials (colours from `PALETTE`).
- `game/decoration.ts` — animated 8×8 cube-grid overlays per tile kind (one shared animator).
- `game/effects.ts` — transient projectiles and teleport swirls.

### Static fallback
Pipeline: `resume.json` + `template.html` → `build.py` (Jinja2) → `public/index.html`.
- `resume.json` — résumé content ([JSON Resume](https://jsonresume.org/schema/) schema).
- `template.html` — Jinja2 template, CSS inlined.
- `build.py` — renders the template; custom filters `format_date` and `format_year`. Delegates avatar
  hex-mosaic generation to `triangulate.py`.
- `public/index.html` — generated; **do not edit by hand**, regenerate with `python3 build.py`.

After editing `resume.json` or `template.html`, run `python3 build.py` to regenerate the fallback HTML.
