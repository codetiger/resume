.DS_Store
__pycache__/
node_modules/
dist/
*.log
.vite/

# Python tool caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Generated by build.py into the assets/ publicDir (Vite copies it into dist/)
assets/resume.html
*.blend1

# Rust level generator build artifacts
level-gen/target/

# Generated level pool (reproducible via `level-gen gen --profile spread`); keep curated ladders
levels/gen-*.json

# Campaign run data (top-K pool + manifest) — transient, reproducible from the seed range
levels/campaigns/
# Browsable rank report
levels/report.md
