# Build output and dependencies
dist/
node_modules/
.vite/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Generated by the Python builder (regenerate with `python3 build.py`)
assets/resume.html

# Generated / curated level data — not hand-edited
src/game/levels.json
levels/

# Rust build artifacts
level-gen/target/

# Hand-authored HTML entry shell with inlined CSS — huge; leave as-is so a
# reformat never churns the inlined markup or risks a visual regression.
index.html

# Jinja2 template — Prettier doesn't understand {% %}/{{ }} and would mangle it.
template.html

# Vendored design references — not code we maintain.
References/
